[pytest]
DJANGO_SETTINGS_MODULE = myproject.settings
python_files = test_*.py
markers =
    asyncio: mark a test as an async test requiring asyncio
asyncio_mode = auto
asyncio_scope = function
filterwarnings =
    ignore::DeprecationWarning:channels\.http:1
